apiVersion: v1
kind: Service
metadata:
  name: todo-app
  labels:
    app: todo-app
    component: web
spec:
  type: ClusterIP
  selector:
    app: todo-app
    component: web
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http
